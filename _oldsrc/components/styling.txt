CSS rules

root element has class like, similar to name of component
overlay-bar

inner elements have classes like
overlay-bar__slot

modifiers are short names, separate classes there

overlay-bar__slot.disabled